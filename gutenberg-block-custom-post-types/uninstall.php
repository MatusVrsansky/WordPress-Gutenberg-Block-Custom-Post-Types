<?php

/**
 * Fired when the plugin Stand Alone is uninstalled.
 *
 * @package Stand Alone
 */

// If uninstall is not called from WordPress, then exit.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	die;
}